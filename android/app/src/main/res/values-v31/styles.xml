<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ splash screen — icon removed so Flutter SplashScreen
         takes over immediately without a jarring icon flash. -->
    <style name="LaunchTheme" parent="@android:style/Theme.SplashScreen">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <!-- No animated icon → native splash shows only the solid bg colour,
             then Flutter's animated SplashScreen fades in on top. -->
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.SplashScreen">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>